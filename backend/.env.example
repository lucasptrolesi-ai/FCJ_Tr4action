# Variáveis de ambiente para produção

# OpenAI
OPENAI_API_KEY=sk-proj-sua-chave-aqui
OPENAI_MODEL=gpt-4o-mini

# Segurança JWT (gere uma chave segura)
# Para gerar: python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET_KEY=sua-chave-jwt-super-secreta-aqui

# Credenciais Admin (ALTERE EM PRODUÇÃO!)
ADMIN_USERNAME=fcj_creator
ADMIN_PASSWORD=sua-senha-forte-aqui

# Ambiente
ENVIRONMENT=production

# CORS (adicione a URL do seu frontend)
ALLOWED_ORIGINS=https://seu-frontend.vercel.app,https://seu-dominio.com

# RAG
RAG_EMBEDDING_MODEL=all-MiniLM-L6-v2
